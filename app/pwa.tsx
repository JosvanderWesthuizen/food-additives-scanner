"use client"

// This component is no longer needed for service worker registration
// as next-pwa handles this automatically
// We keep it as a placeholder in case we need to add PWA-specific functionality later

export function PWARegister() {
  return null
}
